# React app environment variables
# Copy this file to ".env" (do not commit the real .env) and adjust values as needed.

# Base URL of the backend API (no trailing slash).
# Examples:
#   http://localhost:8000
#   https://api.example.com
# Leave empty to use same-origin relative paths.
REACT_APP_API_BASE_URL=http://localhost:8000

# Public site URL used by integrations that need to construct external links/emails.
# For local development with CRA default port:
REACT_APP_SITE_URL=http://localhost:3000
